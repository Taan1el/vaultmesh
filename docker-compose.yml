services:
  vaultmesh:
    build: .
    ports:
      # The API has no authentication, so it is only published on this machine.
      - "127.0.0.1:4005:4005"
    volumes:
      - vaultmesh-data:/app/data
    restart: unless-stopped

volumes:
  vaultmesh-data:
