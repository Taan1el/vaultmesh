# Copy to .env.local (or .env) to override where the Vite dev server
# proxies /api requests. Only needed if the server is running on a port
# other than the default below; Vite loads this file automatically.

VITE_API_TARGET=http://127.0.0.1:4005
